# Terminal.app

ViewKit-based graphical terminal for mochiOS.

Terminal.app starts the system `msh`, forwards keyboard input through its
existing IPC input protocol, and renders the shell's standard output and error
streams in a scrollable window.
